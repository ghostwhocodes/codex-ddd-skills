interface:
  display_name: "TDD"
  short_description: "Build behavior with red-green-refactor"
  default_prompt: "Use $tdd to implement this change test-first with focused regression or behavior coverage."
