interface:
  display_name: "To Issues"
  short_description: "Break a plan into agent-ready implementation issues"
  default_prompt: "Use $to-issues to split this plan into independently implementable vertical-slice issues."
